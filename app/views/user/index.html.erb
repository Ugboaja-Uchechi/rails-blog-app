<%
=begin%>
 <% @users.each do | user|%>
<%= render partial:'shared/user_display', locals: { user: user} %>
<% end %> 
<%
=end%>

<% @users.each do |user| %>
  <%= render partial: "partials/user",  locals: { user: user } %>
<% end %>